generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Intencionalmente vazio por enquanto.
// O primeiro módulo adicionará seus models e enums aqui.

